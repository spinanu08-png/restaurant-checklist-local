# Restaurant Checklist Local

Static local app for the restaurant pre-purchase checklist.

## Run Locally

```powershell
python -m http.server 8001
```

Open:

```text
http://127.0.0.1:8001/
```

The app stores checklist data in the browser with `localStorage`.
